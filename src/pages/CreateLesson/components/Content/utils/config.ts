import { configInterface as configInterfaceOptions } from "./configInterface"

export const config: configInterfaceOptions = {
    attribution: false,
    // autofocus: false,
    placeholderText: 'Tell us about your lesson',
    heightMax: 250,
    heightMin: 300,
    widthMax: 820,
    widthMin: 200,
}